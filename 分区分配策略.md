# 1.基本原则
## 1.1 一个 Partition 在同一个 Consumer Group 中只能被一个消费者独占消费
一个 Partition 只能分配给一个 Consumer（在同一个 Consumer Group 中）.<br>
> 假设有一个 Topic，有 3 个 Partition：
```sql
Topic: user-events
├── Partition 0
├── Partition 1
├── Partition 2
```
> 场景 1：3 个消费者，属于同一个 group
```sql
Consumer Group: group-A
├── Consumer-1 -> Partition 0
├── Consumer-2 -> Partition 1
├── Consumer-3 -> Partition 2
```

> 场景 2：2 个消费者，属于同一个 group
```sql
Consumer Group: group-A
├── Consumer-1 -> Partition 0, 2
├── Consumer-2 -> Partition 1
```
## 1.2 一个 Kafka 消费者（consumer）可以同时处理多个 partition
当消费者数量小于 partition 数量时，一个消费者会被分配多个 partition，它会逐个轮询这些 partition 拉取消息并处理.<br>
> 假设有一个 Topic，有 4 个 Partition：
```sql
Topic: order-events
├── Partition 0
├── Partition 1
├── Partition 2
├── Partition 3
```
> 场景：2 个消费者，属于同一个 Consumer Group A
```sql
Consumer Group A
├── Consumer-1 → Partition 0, 1
├── Consumer-2 → Partition 2, 3
```

# 2. 分区分配策略
## 2.1 Range (RangeAssignor)
- Range 策略会按 每个 Topic 单独处理，该 Topic 的分区按顺序平均地分给所有消费者
-  Topic 数量较多或 partition 数量无法整除时，会导致某些消费者比其他人承担更多分区
### 2.1.1 对每个 Topic 分别处理
将该topic所有 partition进行切片，平均分配给消费者
### 2.1.2 分配均匀情况
一个主题的分区数是消费者个数的倍数<br>
> 两个 Topic A、B，每个有 4 个 Partition，2 个消费者C0、C1
```
C0 : A0  A1  B0  B1
C1 : A2  A3  B2  B3 
```
### 2.1.3 分配不均情况
> 两个 Topic A、B，每个有 3 个 Partition，2 个消费者C0、C1
```
C0 : A0  A1  B0  B1
C1 : A2      B2   
```
## 2.2 RoundRobin(RoundRobinAssignor)
- 一个消费组所有消费者订阅相同的 Topic，分区分配是均匀的
- 一个消费组所有消费者订阅的 Topic是不同的，分区分配可能是不均的
### 2.1.2 分配均匀情况
> 两个 Topic A、B，每个有 3 个 Partition，2 个消费者C0、C1
```
C0 : A0  A2  B1
C1 : A1  B0  B2
```
### 2.1.3 分配不均情况
> 三个 Topic A、B、C，分别有有1、2、3 个 Partition，3个消费者C0、C1、C2<br>
> C0订阅A, C1订阅A、B, C2订阅A、B、C
```
C0 : A0
C1 : B0
C2 : B1  C0  C1  C1
```
## 2.3 Sticky(StickyAssignor)
- 分区的分配要尽可能均匀
- 分区的分配尽可能与上次分配的保持相同
- 第一个目标优先于第二个目标，“尽量不动就不动”
### 2.3.1 为什么需要 Sticky 分配策略
Kafka 的 RangeAssignor 和 RoundRobinAssignor 每次 rebalance（重平衡）都会重新分配所有 partition。这有两个问题：
- 影响顺序消费：某个消费者原本负责的 partition 突然换人，顺序被打乱。
- 浪费资源：Kafka 会清空原 consumer 的本地缓存，重新加载、重新 seek offset，带来性能开销。
### 2.3.2 示例
> 3个消费者C0、C1、C2， 都订阅了 A、B、C、D 4个主题，每个主题2个分区<br>
```
C0 : A0  B1  D0
C1 : A1  C0  D1
C2 : B0  C1
```
> C1脱离了消费组<br>
若采用RoundRobin:
```
C0 : A0  B0  C0  D0
C1 :  -   -   -   -
C2 : A1  B1  C1  D1
```
采用Sticky:
```
C0 : A0  B1  D0    C0    
C1 :  -   -   -   -
C2 : B0  C1    A1  D1
```
<mark>Sticky比RoundRobin减少了分区变动</mark>
## 2.4 CooperativeSticky(CooperativeStickyAssignor)
CooperativeStickyAssignor 是 Kafka 中最智能、最推荐的分区分配策略，结合了：
- Sticky 的粘性分配（减少不必要的变动）
- Cooperative 的协作式重平衡（避免全部消费者中断）<br>
实现最小扰动、不中断消费的高可用分区分配策略
