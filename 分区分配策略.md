# 1.基本原则
## 1.1 一个 Partition 在同一个 Consumer Group 中只能被一个消费者独占消费
一个 Partition 只能分配给一个 Consumer（在同一个 Consumer Group 中）.<br>
> 假设有一个 Topic，有 3 个 Partition：
```sql
Topic: user-events
├── Partition 0
├── Partition 1
├── Partition 2
```
> 场景 1：3 个消费者，属于同一个 group
```sql
Consumer Group: group-A
├── Consumer-1 -> Partition 0
├── Consumer-2 -> Partition 1
├── Consumer-3 -> Partition 2
```

> 场景 2：2 个消费者，属于同一个 group
```sql
Consumer Group: group-A
├── Consumer-1 -> Partition 0, 2
├── Consumer-2 -> Partition 1
```
## 1.2 一个 Kafka 消费者（consumer）可以同时处理多个 partition
当消费者数量小于 partition 数量时，一个消费者会被分配多个 partition，它会逐个轮询这些 partition 拉取消息并处理.<br>
> 假设有一个 Topic，有 4 个 Partition：
```sql
Topic: order-events
├── Partition 0
├── Partition 1
├── Partition 2
├── Partition 3
```
> 场景：2 个消费者，属于同一个 Consumer Group A
```sql
Consumer Group A
├── Consumer-1 → Partition 0, 1
├── Consumer-2 → Partition 2, 3
```

# 2. 分区分配策略
## 2.1 Range (RangeAssignor)
- Range 策略会按 每个 Topic 单独处理，该 Topic 的分区按顺序平均地分给所有消费者
-  Topic 数量较多或 partition 数量无法整除时，会导致某些消费者比其他人承担更多分区
### 2.1.1 对每个 Topic 分别处理
将该topic所有 partition进行切片，平均分配给消费者
### 2.1.2 分配均匀情况
一个主题的分区数是消费者个数的倍数<br>
> 两个 Topic A、B，每个有 4 个 Partition，2 个消费者C0、C1
```
C0 : A0  A1  B0  B1
C1 : A2  A3  B2  B3 
```
### 2.1.2 分配不均情况
> 两个 Topic A、B，每个有 3 个 Partition，2 个消费者C0、C1
```
C0 : A0  A1  B0  B1
C1 : A2      B2   
```
## 2.2 Range (RangeAssignor)
